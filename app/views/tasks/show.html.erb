<div class="container">
  <h1><%= @task.title %></h1>
  <p><%= @task.details %></p>
  <p><%= @task.completed ? "Completed" : "Incomplete" %></p>
  <p><%= link_to "Update Task", edit_task_path(@task), class: "btn btn-primary" %></p>
  <p><%= link_to 'Back to list', tasks_path %></p>
</div>
